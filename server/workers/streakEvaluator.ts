// Streak evaluator worker — runs hourly via cron
// Checks all ACTIVE streaks for at-risk/broken status
// Implemented in Slice 4
export async function runStreakEvaluator(): Promise<void> {
  throw new Error('Streak evaluator not yet implemented — coming in Slice 4')
}
