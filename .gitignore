node_modules
build
dist
.cache
.DS_Store

# Environment variables — NEVER commit these
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Next.js build output
app/web/.next/
app/web/out/

# Prisma generated client
node_modules/.prisma/

# Root-level package-lock (app/web/package-lock.json is the real one)
/package-lock.json

# Test artifacts
playwright-report/
test-results/

# Superpowers tool (ephemeral brainstorm/server files)
.superpowers/
